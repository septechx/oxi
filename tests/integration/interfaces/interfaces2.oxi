interface Maker {
    fn new(v: i32) Self;
}

struct S {
    v: i32,
}

impl Maker for S {
    fn new(v: i32) Self {
        return S { v };
    }
}

pub fn main() i32 {
    let s = S::new(10);
    return s.v;
}
