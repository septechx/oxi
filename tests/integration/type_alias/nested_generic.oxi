type Vec2<T> = (T, T);

struct Holder {
    v: Vec2::<i32>,
}

type Wrap<T> = T;

struct Holder2 {
    w: Wrap::<u32>,
}

pub fn main() void {
    let h = Holder { v: (1, 2) };
    let _ = h;
    let h2 = Holder2 { w: 3 };
    let _ = h2.w + 1;
}
