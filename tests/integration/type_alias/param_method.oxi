struct Counter {
    n: u32,

    fn get(self: &mut Self) u32 {
        self.n
    }
}

type C = Counter;

fn use_counter(c: C) u32 {
    c.get()
}

pub fn main() void {
    let x = use_counter(Counter { n: 9 });
    let _ = x;
}
