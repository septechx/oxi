pub fn main() bool {
    let a = 10;
    let b = 5;
    let c = a < b;
    let d = a > b;
    let e = a <= b;
    let f = a >= b;
    let g = a == b;
    let h = a != b;
    c && d && e && f && g && h
}
