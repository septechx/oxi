// @expect-error type_mismatch

trait Bar {
    fn make() Self;
}

struct Foo<T> {
    data: T,
}

impl Bar for Foo::<i32> {
    fn make() Self {
        Foo { data: 1 }
    }
}

pub fn main() void {
    let x: Foo::<bool> = Foo::make();
    let _ = x;
}
