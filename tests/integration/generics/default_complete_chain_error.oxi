// @expect-error type_mismatch

struct Pair<T = i64, U = T> {
    first: T,
    second: U,
}

pub fn main() void {
    // `Pair::<u32>` completes `U = T` to `u32`, so `second` must not be `i64`.
    let p = Pair::<u32> { first: 12, second: 24 };
    let second: i64 = p.second;
}